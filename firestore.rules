rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /sustainTechConCollections/{docId} {
      allow read: if true;
      allow write: if request.auth != null;

      match /tickets/{ticketId} {
        allow read: if true;
        allow write: if request.auth != null;
      }
    }
  }
}
